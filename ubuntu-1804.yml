# g++
# Build your C++ projects with g++ and GNU Make.
# https://aka.ms/yaml

pool:
    vmImage: 'Ubuntu-20.04'
    variables:
      cc: gcc
      cxx: g++
    
steps:
- task: CmdLine@2
  inputs:
    script: |
       g++ --version